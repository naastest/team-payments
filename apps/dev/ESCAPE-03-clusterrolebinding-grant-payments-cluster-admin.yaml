# ESCAPE ATTEMPT 3: Bind the payments-dev group to cluster-admin ClusterRole.
# Expected block: ClusterRoleBinding is cluster-scoped; clusterResourceWhitelist only allows Namespace.
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: payments-dev-cluster-admin-escalation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
  - kind: Group
    apiGroup: rbac.authorization.k8s.io
    name: naas-payments-dev
