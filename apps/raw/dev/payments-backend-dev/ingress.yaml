apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neon-billboard
  namespace: payments-backend-dev
  labels:
    app.kubernetes.io/name: neon-billboard
    naas.io/team: payments
    naas.io/env: dev
spec:
  ingressClassName: nginx
  rules:
    - host: neon-billboard.naas.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: neon-billboard
                port:
                  number: 80
